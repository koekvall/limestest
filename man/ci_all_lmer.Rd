% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidence_intervals.R
\name{ci_all_lmer}
\alias{ci_all_lmer}
\title{Confidence intervals for covariance parameters}
\usage{
ci_all_lmer(lmerfit, test_idx = NULL, level = 0.95, onestep = FALSE, ...)
}
\arguments{
\item{lmerfit}{An \code{lmerMod} object from fitting a linear mixed model
using \code{lme4::lmer}.}

\item{test_idx}{Integer vector specifying which parameters to compute CIs for.
If \code{NULL} (default), CIs are computed for all covariance parameters
including the error variance.}

\item{level}{Numeric confidence level in (0, 1). Default is \code{0.95}.}

\item{...}{Additional arguments passed to \code{\link{ci_lmer}}.}
}
\value{
A matrix with one row per parameter and columns \code{lower} and
\code{upper}. Row names are of the form \code{"var | grouping_factor"} or
\code{"var1:var2 | grouping_factor"} for covariance parameters.
}
\description{
Computes score-based confidence intervals for each covariance parameter in a
linear mixed model fitted with lme4, by calling \code{\link{ci_lmer}} for
each parameter.
}
\examples{
\donttest{
library(lme4)
fit <- lmer(Reaction ~ Days + (Days | Subject), data = sleepstudy)

# 95\% CIs for all random-effect variance/covariance parameters
ci_all_lmer(fit)
}
}
\seealso{
\code{\link{ci_lmer}}
}
